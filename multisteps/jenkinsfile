pipeline {
    agent any
    
    stages {
        stage('Hello World') {
            steps {
                echo 'Hello, World!'
            }
        }
        
        stage('Build') {
            steps {
                echo 'Building...'
                echo "PREVENT_BUILD=${env.PREVENT_BUILD}"
                echo "REPO=${env.REPO}"
                // Add your build steps here
            }
        }
        
        stage('Test') {
            steps {
                echo 'Running tests...'
                // Add your test steps here
            }
        }
    }
}